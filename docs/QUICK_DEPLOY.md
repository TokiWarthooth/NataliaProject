# 🚀 Быстрый деплой бота

## Вариант 1: Railway (Самый простой, бесплатно)

### Шаги:
1. Зайдите на https://railway.app/ и войдите через GitHub
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. В разделе "Variables" добавьте:
   - `BOT_TOKEN` = ваш токен
   - `LAWYER_CHAT_ID` = ваш chat ID
5. Готово! Бот автоматически развернется

**Плюсы:** Бесплатно, очень просто, автоматический деплой при push в GitHub

---

## Вариант 2: VPS (Рекомендуется для продакшна)

### Где арендовать:
- **Hetzner** - от €4/мес (самый дешевый) - https://www.hetzner.com/
- **Timeweb** - от 200₽/мес (российский) - https://timeweb.com/
- **DigitalOcean** - от $6/мес - https://www.digitalocean.com/

### Быстрый деплой на VPS:

```bash
# 1. Подключитесь к серверу
ssh root@your-server-ip

# 2. Установите необходимое
apt update && apt install -y python3 python3-pip python3-venv git

# 3. Клонируйте репозиторий
git clone https://github.com/your-username/your-repo.git telegram-bot
cd telegram-bot

# 4. Создайте .env файл
nano .env
# Добавьте:
# BOT_TOKEN=ваш_токен
# LAWYER_CHAT_ID=ваш_chat_id

# 5. Установите и запустите
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 6. Настройте автозапуск (скопируйте telegram-bot.service в /etc/systemd/system/)
sudo cp telegram-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

### Настройка автоматического деплоя:

1. В GitHub: Settings → Secrets → Actions
2. Добавьте секреты:
   - `VPS_HOST` - IP сервера
   - `VPS_USER` - пользователь (root или другой)
   - `VPS_SSH_KEY` - приватный SSH ключ
   - `VPS_DEPLOY_PATH` - путь к проекту (например, `/root/telegram-bot`)

3. Теперь при каждом `git push` бот автоматически обновится!

---

## Вариант 3: Render (Бесплатно)

1. Зайдите на https://render.com/
2. New → Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - Build: `pip install -r requirements.txt`
   - Start: `python bot.py`
5. Добавьте переменные окружения
6. Deploy!

---

## Что выбрать?

- **Для тестирования/начала:** Railway (бесплатно, 2 минуты на настройку)
- **Для продакшна:** VPS (надежнее, больше контроля, от $5/мес)
- **Бюджетный вариант:** Hetzner VPS (€4/мес) или Timeweb (200₽/мес)

---

## После деплоя

Проверьте работу бота:
1. Найдите бота в Telegram
2. Отправьте `/start`
3. Проверьте все функции

**Логи:**
- Railway/Render: в веб-интерфейсе
- VPS: `sudo journalctl -u telegram-bot -f`

