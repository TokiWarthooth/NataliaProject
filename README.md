# Telegram Bot для юридических услуг

Бот для приема заявок на юридические консультации.

## Функционал

- ✅ Приветствие при запуске
- ✅ Пользовательское соглашение
- ✅ Выбор сферы юридической услуги (семейное право, имущественное, развод, IT право и др.)
- ✅ Форма заявки (описание проблемы, телефон, email)
- ✅ Автоматическая отправка заявки юристу в Telegram
- ✅ Подтверждение пользователю об успешной отправке

## Установка и запуск

### Локальная разработка

1. Установите Python 3.11 или выше

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` и заполните его:
```bash
BOT_TOKEN=8564606241:AAEfxSP16OO7LiJRDe0nLUjDD3dFGKldAlw
LAWYER_CHAT_ID=ваш_chat_id_юриста
```

**Как получить LAWYER_CHAT_ID:**
- Напишите боту @userinfobot в Telegram
- Он покажет ваш Chat ID (число, например: 123456789)
- Вставьте это число в `.env` файл

4. Запустите бота:
```bash
python bot.py
```

### Запуск через Docker

1. Создайте `.env` файл (см. выше)

2. Запустите через docker-compose:
```bash
docker-compose up -d
```

3. Просмотр логов:
```bash
docker-compose logs -f
```

4. Остановка:
```bash
docker-compose down
```

## Отладка в Cursor/VS Code

1. Убедитесь, что установлено расширение Python (обычно уже установлено)

2. Откройте файл `bot.py`

3. Установите breakpoint (точку останова) - кликните слева от номера строки

4. Нажмите **F5** или выберите "Run and Debug" в меню

5. Бот запустится в режиме отладки, и выполнение остановится на breakpoint

**Полезные команды отладки:**
- **F5** - продолжить выполнение
- **F10** - шаг через (step over)
- **F11** - шаг внутрь (step into)
- **Shift+F11** - шаг наружу (step out)
- **Ctrl+Shift+F5** - перезапустить отладку

## Развертывание на сервере

### Вариант 1: Docker (рекомендуется)

1. Скопируйте все файлы на сервер
2. Создайте `.env` файл с токенами
3. Запустите: `docker-compose up -d`

### Вариант 2: Python напрямую

1. Установите Python 3.11+ на сервере
2. Установите зависимости: `pip install -r requirements.txt`
3. Создайте `.env` файл
4. Запустите через systemd или screen/tmux:
```bash
# Через screen (простой способ)
screen -S telegram-bot
python bot.py
# Нажмите Ctrl+A, затем D для отсоединения
```

## Развертывание в продакшн

📖 **Подробная инструкция:** См. [DEPLOY.md](DEPLOY.md)

### Быстрый старт:

**Вариант 1: Railway (бесплатно, просто)**
1. Зайдите на https://railway.app/
2. Создайте проект из GitHub репозитория
3. Добавьте переменные окружения (BOT_TOKEN, LAWYER_CHAT_ID)
4. Готово!

**Вариант 2: VPS (рекомендуется для продакшна)**
1. Арендуйте VPS (Hetzner, DigitalOcean, Timeweb)
2. Следуйте инструкциям в [DEPLOY.md](DEPLOY.md)
3. Настройте автоматический деплой через GitHub Actions

### Автоматический деплой

При каждом `git push` в ветку `main` бот автоматически обновится на сервере через GitHub Actions!

## Структура проекта

```
NataliaProject/
├── src/                      # Исходный код бота
│   ├── __init__.py
│   ├── bot.py               # Главный файл бота
│   ├── config.py            # Конфигурация и константы
│   ├── handlers.py          # Обработчики команд
│   ├── keyboards.py         # Клавиатуры
│   └── application_service.py  # Сервис заявок
├── configs/                 # Конфигурационные файлы
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── Procfile
│   ├── railway.json
│   └── render.yaml
├── deploy/                  # Файлы для развертывания
│   ├── deploy.sh
│   └── telegram-bot.service
├── docs/                    # Документация
│   ├── DEPLOY.md
│   ├── QUICK_DEPLOY.md
│   └── QUICKSTART.md
├── .github/workflows/       # GitHub Actions
│   └── deploy.yml
├── bot.py                   # Точка входа (запуск бота)
├── requirements.txt         # Зависимости Python
├── .env                     # Переменные окружения (не коммитится)
└── README.md
```

## Примечания

- Токен бота уже указан в примере (можно использовать)
- Не забудьте указать `LAWYER_CHAT_ID` для получения заявок
- Для продакшена рекомендуется использовать базу данных вместо словаря `user_data`

